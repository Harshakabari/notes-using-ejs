<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com" ></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block px-3 w-full outline-none rounded-md py-3 bg-zinc-800" name="title" type="text" placeholder="Title goes">
                <textarea class="block mt-2 w-full outline-none rounded-md  px-3 py-3 bg-zinc-800 resize-none" name="details" placeholder="Write your task details"></textarea>
                <input type="submit" class="mt-2 bg-blue-600 rounded-md p-1 " value="create task"></input>
            </form>
        </div>
        <div class="tasks p-10 gap-3 flex-wrap flex inline-block">

            <% if(files.length >0){ %>
                <% files.forEach(function(val){ %>
                    <div class="task  w-72 px-3 py-4 bg-zinc-800 ">
                        <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
                        <div class="flex width-full justify-between">
                            <a class="text-blue-500 inline-block " href="/files/<%= val %>">Read more.</a>
                            <a class="text-zinc-600" href="/edit/<%= val %>">Edit Filename</a>
                        </div>
                    </div>
                <% })%>
            <% } else { %>
            <h3 class="text-zinc-600">no task yet</h3>
            <% }%>    
        </div>
    </div>
    
</body>
</html>